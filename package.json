{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "A full-scale, self-hostable library management system built with modern technologies.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "test:api": "npx playwright test __tests__/api",
    "test:e2e": "npx playwright test __tests__/e2e",
    "test:all": "npx playwright test",
    "seed": "cd server && npm run seed"
  },
  "keywords": ["library", "management", "fullstack", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
